import { MarkCompleteDirective } from './mark-complete.directive';

describe('MarkCompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkCompleteDirective();
    expect(directive).toBeTruthy();
  });
});
